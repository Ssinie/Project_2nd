<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="main">
	
	<select id="wishCheck" parameterType="productDTO" resultType="Integer">
		select avg(clicked) as clicked from wishlist where id=#{id} and num=#{num}
	</select>
	
	<insert id="wishInsert" parameterType="productDTO">
		insert into wishlist(id, num, clicked) values(#{id}, #{num}, 1)
	</insert>
	
	<delete id="wishDelete">
		delete from wishlist where id=#{id} and num=#{num}
	</delete>
	
	<select id="getPdList" resultType="productDTO">
		<![CDATA[select name, imgurl from (select name, imgurl from test_item order by view_count desc) where rownum <= 6]]>
	</select>
	
</mapper>