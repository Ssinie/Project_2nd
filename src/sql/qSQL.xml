<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--정보 압력 insert.-->
<!--파라미터 받을 시 오류날 때, jdbcType = VARCHAR 추가.-->
<mapper namespace = "question">
	<insert id = "qInsert" parameterType = "bean.question.qDTO">
		insert into question values(
		#{nick, jdbcType = VARCHAR},
		#{age},
		#{weight},
		#{height},
		#{gender, jdbcType = VARCHAR},
		sysdate,
		#{qcount},
		#{ref}
		)
	</insert>
	<!--<insert id = "insert2" parameterType = "bean.question.q2DTO">
		insert into question2 values(
		#{intr, jdbcType = VARCHAR},
		sysdate,
		#{ref},
		#{bld, jdbcType = VARCHAR}
		)
	</insert>-->
	<insert id = "qcInsert" parameterType = "bean.question.qcDTO">
		insert into question2 values(
		#{title, jdbcType = VARCHAR},
		#{contents, jdbcType = VARCHAR},
		sysdate,
		#{ref}
		)
	</insert>
</mapper>
